[profile.default]
# Run every selected test so one failure does not hide unrelated regressions.
fail-fast = false
status-level = "fail"
final-status-level = "fail"
# Native rendering tests require an explicitly selected GPU-backed run.
default-filter = "not (package(=byte-engine-ghi) & kind(=test) & binary(=rendering))"
