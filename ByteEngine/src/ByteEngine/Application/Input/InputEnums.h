#pragma once

#include "ByteEngine/Core.h"

enum JoystickButtons
{
	RightMenuButton,
	LeftMenuButton,

	TopFaceButton,
	RightFaceButton,
	BottomFaceButton,
	LeftFaceButton,

	TopDPadButton,
	RightDPadButton,
	BottomDPadButton,
	LeftDPadButton,

	RightBackButton,
	LeftBackButton,
};
