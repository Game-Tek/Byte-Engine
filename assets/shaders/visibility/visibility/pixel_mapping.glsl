layout(local_size_x=32, local_size_y=32) in;
void main() {
	if (gl_)
	uint index = atomicAdd(material_start[pixel[0]], 1);
	material_xy[index] = vec2u16(gl_GlobalInvocationID.x, gl_GlobalInvocationID.y);
}