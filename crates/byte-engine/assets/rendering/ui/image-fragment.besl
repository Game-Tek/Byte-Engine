image_texture: descriptor<Texture2D, 0, read>;
in_uv: input<vec2f, 0>;
in_opacity: input<f32, 1>;
out_color_attachment: output<vec4f, 0>;
main: fn() -> void {
	let color: vec4f = sample(image_texture, in_uv);
	out_color_attachment = vec4f(color.x, color.y, color.z, color.w * in_opacity);
}
