main: fn (
	input: StageInput,
	pipeline_input: interface {
		instance_index: u32,
		primitive_index: u32,
		uv: vec2f16,
	},
) -> output {
	primitive_index: u32,
	instance_id: u32,
} {
	return {
		primitive_index: pipeline_input.primitive_index,
		instance_id: pipeline_input.instance_index,
	};
}
