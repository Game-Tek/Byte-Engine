[package]
name = "gi"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
byte_engine = { path = "../.." }
log = "0.4.20"
maths-rs = "0.2.6"

[profile.release]
debug = true
